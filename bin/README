1.文件说明
README             -- 使用指南
app.sh             -- 包含启动、停止、重启等命令
setenv.sh          -- 设置jvm等个性化配置参数
deploy_apm.sh      -- skywalking agent部署脚本

2.使用说明
app.sh start       -- 启动应用，并加载config/目录下的配置文件
app.sh start dev   -- 启动应用，并加载config/dev/目录下的配置文件
app.sh start sit   -- 启动应用，并加载config/sit/目录下的配置文件
app.sh start uat   -- 启动应用，并加载config/uat/目录下的配置文件
app.sh start prd   -- 启动应用，并加载config/prd/目录下的配置文件

app.sh status      -- 查看应用状态
app.sh stop        -- 停止应用
app.sh restart     -- 重启应用, 并加载config/目录下的配置文件
app.sh restart dev -- 重启应用, 并加载config/dev目录下的配置文件
app.sh restart sit -- 启动应用，并加载config/sit/目录下的配置文件
app.sh restart uat -- 启动应用，并加载config/uat/目录下的配置文件
app.sh restart prd -- 启动应用，并加载config/prd/目录下的配置文件